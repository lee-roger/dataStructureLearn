### 稀疏数组的定义
当一个数组中大部分元素为0（或者其他无效数字时），可以将数组转换成稀疏数组进行存储

### 二维数组转稀疏数组的思路
 1. 遍历原始的二维数组，得到有效数据的个数sum
 2. 根据sum就可以创建稀疏数组sparseArray int[sum+1\][3\]
 3. 将原始二维数组的有效数据存入到稀疏数组中

### 稀疏数组转原始数组的思路
1. 先读取稀疏数组的第一行（稀疏数组的第一行存放的是原始数组的大小，以及有效元素的个数），很根据第一行的数据创建原始的二维数组
2. 再读取稀疏数组后几行的数据，并赋值给原始的二维数组
